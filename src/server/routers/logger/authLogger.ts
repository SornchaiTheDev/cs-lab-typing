import { router, procedure } from "@/server/trpc";

export const AuthLoggerRouter = router({
  // authLogger : procedure.meta({openapi : {method : "POST" , path : "/auth-logger"}}).input().output().mutation(({input}) => {
});
